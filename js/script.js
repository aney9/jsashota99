let massiv = [];function add() {  const surname = document.getElementById('Surname');  const name = document.getElementById('Name');  const middlename = document.getElementById('Middlename');  const fanid = document.getElementById('Fanid');  const phone = document.getElementById('Phone');  const sektor = document.getElementById('Sektor');  const ryad = document.getElementById('Ryad');  const mesto = document.getElementById('Mesto');  const Surname = surname.value.trim();  const Name = name.value.trim();  const Middlename = middlename.value.trim();  const Fanid = parseInt(fanid.value);  const Phone = phone.value.trim();  const Sektor = parseInt(sektor.value);  const Ryad = parseInt(ryad.value);  const Mesto = parseInt(mesto.value);  if (!Surname || !Name || !Middlename || isNaN(Fanid) || Fanid <= 0 || !Phone || isNaN(Sektor) || Sektor <= 0 || isNaN(Ryad) || Ryad <= 0 || isNaN(Mesto) || Mesto <= 0) {    alert('Ошибка ввода');    return;  }  const tickets = {    Surname: Surname,    Name: Name,    Middlename: Middlename,    Fanid: Fanid,    Phone: Phone,    Sektor: Sektor,    Ryad: Ryad,    Mesto: Mesto,    displayInfo: function () {        return `Имя: ${this.Name} фамилия: ${this.Surname} отчество: ${this.Middlename} FanId: ${this.Fanid} Телефон: ${this.Phone} Сектор: ${this.Sektor} Ряд: ${this.Ryad} Место: ${this.Mesto}`;    }  };  massiv.push(tickets);  update();  surname.value = '';  name.value = '';  middlename.value = '';  fanid.value = '';  phone.value = '';  sektor.value = '';  ryad.value = '';  mesto.value = '';}function deletee(index){    massiv.splice(index, 1);    update();}function edit(index){    const tickets = massiv[index];    const Surnamee = prompt('фамилия: ', tickets.Surname);    const Namee = prompt('имя: ', tickets.Name);    const Middlenamee = prompt('отчество: ', tickets.Middlename);    const Fanidd = prompt('FanID: ', tickets.Fanid);    const Phonee = prompt('телефон: ', tickets.Phone);    const Sektorr = prompt('сектор: ', tickets.Sektor);    const Ryadd = prompt('ряд: ', tickets.Ryad);    const Mestoo = prompt('место: ', tickets.Mesto);    if (!Surnamee || !Namee || !Middlenamee || isNaN(Fanidd) || Fanidd <= 0 || !Phonee || isNaN(Sektorr) || Sektorr <= 0 || isNaN(Ryadd) || Ryadd <= 0 || isNaN(Mestoo) || Mestoo <= 0) {        alert('Ошибка ввода');        return;    }  tickets.Surname = Surnamee;  tickets.Name = Namee;  tickets.Middlename = Middlenamee;  tickets.Fanid = Fanidd;  tickets.Phone = Phonee;  tickets.Sektor = Sektorr;  tickets.Ryad = Ryadd;  tickets.Mesto = Mestoo;  update();}function update() {  const list = document.getElementById('list');  list.innerHTML = '';  massiv.forEach((tickets, index) => {    const li = document.createElement('li');    li.classList.add('ticket-item');    li.textContent = tickets.displayInfo();    const editButton = document.createElement('button');    editButton.textContent = 'Изменить';    editButton.onclick = function () {      edit(index);    };    li.appendChild(editButton);    const deleteButton = document.createElement('button');    deleteButton.textContent = 'Удалить';    deleteButton.onclick = function () {      deletee(index);    };    li.appendChild(deleteButton);    list.appendChild(li);  });}